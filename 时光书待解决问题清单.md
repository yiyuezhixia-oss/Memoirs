# 时光书 · 待解决问题清单

> 规则：每完成一项，在该项行首把 `[ ]` 改为 `[x]` 并注明完成日期/提交。
> 最后更新：2026-08-06

## 大模型 API 接入（DeepSeek）

- [x] 提取 DeepSeek 官方文档要点（create-chat-completion / json_mode / multi_round_chat） → 2026-08-06
- [x] 在 `services/api.ts` 接入 DeepSeek 真实接口（OpenAI 兼容：base=https://api.deepseek.com/chat/completions，鉴权 Bearer） → 2026-08-06
- [x] Admin 后台新增「大模型 API 接入」面板（模式切换 mock/llm/coze + 表单 + 保存 + 真实自检） → 2026-08-06
- [x] 提供「一键接入 DeepSeek」预填（地址/模型/临时 key），避免硬编码私密 key → 2026-08-06
- [x] 三个生成入口共享同一 DeepSeek API，但角色设定（人设/语气/最终产出结构）各自不同 → 2026-08-06
- [x] `prepareGeneration` 把 `role.systemPrompt` 作为 system 消息带入模型，使文章/漫画/日记卡返回语气与结构不同 → 2026-08-06
- [ ] 评估并接入 DeepSeek 思考模式（thinking / reasoning_effort）以优化长文质量
- [x] 处理 DeepSeek 多模态：确认图片不支持，AI整理回忆带图时在 `messageContent` 降级为文字描述 → 2026-08-06
- [ ] 流式输出（SSE）替代当前一次性 fetch，提升生成体验

## AI 整理回忆 流程

- [x] 多轮引导 + 提取时间/地点/人物/事件/原话/细节/情绪 → 依赖 chatWithRole + 角色人格 → 2026-08-06
- [x] 生成时间轴草稿（自然语言或 JSON） → prepareMemory → 2026-08-06
- [x] 同一天只保留一条、合并新旧、按日期排序 → organizeSession 逻辑 → 2026-08-06
- [x] 带照片场景：DeepSeek 不支持图片，已在 `messageContent` 降级为文字描述（提示模型用户会说明照片内容） → 2026-08-06

## 文章 / 漫画 / 日记卡 流程

- [x] 进入生成页 → 带入时间轴记录 → 多轮补充 → 整理为结构化规格 → 2026-08-06
- [x] Coze 工作流真实接入（国内站 api.coze.cn/v1/workflow/run，文件上传拿 file_id 再传工作流，已用测试 key+测试工作流「文图生图」7670440935159447604 端到端验证通过，三个入口暂共用该测试 workflow） → 2026-08-06
- [ ] 生成结果前端 UI 渲染（文章排版 / 漫画分镜 / 日记卡）打磨
- [ ] 我的页面读取并展示已保存作品

## 其他

- [ ] 清理 `services/workflow.ts` 废弃旧 mock（仅保留 services/api）— 已移除 import，文件可删
- [ ] 角色管理支持新增/删除自定义入口（当前仅 4 个固定 RoleId）
- [ ] 每个生成入口可绑定独立模型/key（当前全局共用一套 llm 配置）
- [ ] 角色人格模板库（预设"温柔姐姐/理性编辑"等）

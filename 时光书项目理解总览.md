# 时光书 · 项目理解总览（全文档重读后的整合理解）

> 整合自：OPC 商业画布、功能实现、前端设计、用户路径、对抗性审查、UI 提示词、资产提示词、调研规划十条 共 8 份文档。
> 用途：一句话讲清项目是什么、核心功能、前后端架构、技术实现，并对每个文档做摘要 + 给出整体看法。

---

## 一、这个项目到底是什么

**一句话**：时光书是一款面向母亲及父母群体的「亲子回忆内容生成工具」。父母用一张照片、一句话或一段聊天，把孩子的成长瞬间记录下来，系统自动整理成「时间日志」，再生成**时光日记卡、温柔抒情文章、故事散文、亲子漫画、插图预览**，可保存、分享、长期回看。

**核心不是教父母写作，而是把记录门槛压到最低**：你只表达一点点，产品负责把零碎的爱、记忆、期盼、夸赞，整理成能珍藏的亲子回忆。

**产品闭环**：
```
孩子档案 → 照片/一句话或聊天采集 → 时间日志 → 生成方案确认(多轮聊天) → 日记卡/文章/漫画/插图 → 预览/导出
```

---

## 二、核心功能（按文档归纳）

| 功能 | 说明 | 来源 |
|---|---|---|
| 时间轴沉淀 | 倒序展示成长日志，每条是书页卡片（日期/照片/标题/摘要） | 时间轴首页 |
| 手动新增 | 右下角「+」进新增页，支持年/年月/日期/区间四种日期精度 + 多图 + 文字 | 新增回忆页 |
| AI 整理回忆 | 多轮聊天引导讲细节（时间/事件/孩子的话/父母感受），整理成时间日志草稿，确认后写入 | 记忆整理聊天页 + 草稿确认 |
| 三层去重 | 消息状态( pending→processed ) + 批次( process_batch_id ) + 内容指纹( content_hash )，防事件级重复 | 日志整理章节 |
| 生成首页 | 独立创作区，4 入口：AI整理回忆 / 文章 / 漫画 / 日记卡 | 生成首页 |
| 文章生成 | 多轮聊天确认写给谁/用途/想说的话/文风 → 本页预览书页文章 | 文章生成页 |
| 漫画生成 | 聊天确认画面/动作/情绪/参考图 → 固定风格亲子插画（**不承诺真人还原**）→ 本页预览 | 漫画生成页 |
| 日记卡生成 | 聊天确认主图/标题/短句/氛围 → 竖版可分享卡片 → 本页预览 | 日记卡生成页 |
| 我的页面 | 只查看已保存作品，按文章/漫画/日记卡分类（**不作生成入口**） | 我的页面 |
| 多轮聊天确认 | 三个生成页都以聊天为主体，不是选择题表单；确认后**当前页**切换预览，不跳独立页 | 贯穿所有生成页 |

---

## 三、前后端架构（怎么分工的）

### 前端（H5 Demo / 微信小程序）
- 移动端 H5，最大宽度 430px、桌面居中（`AppShell`）。
- 底部主导航固定：**时间轴 / 生成 / 我的**（职责严格隔离，对抗性审查 P0）。
- 16 个核心组件（`TimelineCard`、`MemoryChatPanel`、`GenerationWorkspace`、`InlinePreviewPanel` 等）。
- 数据层 Demo 阶段用 **localStorage**：`timelineEntries`、`memoryChatMessages`、`generationTasks`。
- 路由：`/`、`/entry/new`、`/memory-chat`、`/generate`、`/generate/article|comic|diary-card`、`/mine`。
- 稳定性约束：`USE_MOCK_WORKFLOWS=true` 时所有工作流返回本地 mock，现场演示跑通闭环再切真实。
- 视觉系统：柔和粉白/浅紫/奶油黄/薄荷绿/浅蓝，色 Token 已定义；Q 版亲子插画 + 书页/相纸/便签风。

### 后端（扣子 Coze 工作流，云端 SaaS）
- 5 个能力节点（工作流），前端只做入口/确认/预览：
  1. `runNarrativeChatWorkflow` — 叙事聊天（轻量实时，只产追问）
  2. `runTimelineExtractWorkflow` — 日志整理（聊天→日志草稿，重但异步）
  3. `runArticleWorkflow` — 文章生成
  4. `runComicWorkflow` — 漫画生成
  5. `runDiaryCardWorkflow` — 日记卡生成
- **关键架构决策**：Coze 无状态、云端，读不到本地存储；数据进出只有「推模式」——前端把 entry 序列化进工作流开始节点入参（支持 Object/Array JSON），这是 Demo 唯一可行路径。Coze 本身不存业务数据（隐私边界）。
- 编排：前端做会话状态机（聊天叙事→想法确认→生成预览），5 个工作流作为被按需调用的能力节点，非互相独立。
- 隐私边界：照片与故事仅用于本次生成与用户归档，不训练、不对外分发。

---

## 四、技术实现要点（落到代码层）

- **Demo 排期**：两天。Day1 搭页面+本地存储+时间轴+新增+聊天+草稿确认；Day2 去重+生成首页+三生成工作台+本页预览+演示数据/空态/错误态。
- **数据结构**：
  - `timeline_entry`：`date_type`( year/month/day/range )、`source`( manual/ai )、`content_hash`、图片数组。
  - `message`：`process_status`( pending/processing/processed/ignored )、`processed_batch_id`。
  - `generationTask`：`type`( article/comic/diary_card )、`status`( pending/generating/done/failed )、`sourceEntryId`、`result`。
- **去重三层**：状态去重（只交 pending）→ 批次去重（写成功后标 processed）→ 指纹去重（同日+同标题+同核心内容 hash 已存在则提示「可能已整理过」）。仅确认写入后才标 processed，失败/取消恢复 pending。
- **图片上传坑**：Demo 用本地 blob 预览即可；但真实接 Coze 时图片不能传 blob URL，必须先上传成工作流可访问的 URL——这一步需明确（谁上传、传到哪）。
- **验收**：10 条 Demo 验收标准 + 前端 9 条 + 对抗性审查 6 条验收场景，合计构成完整验收清单。

---

## 五、逐文档摘要（8 份）

### 1. 时光书OPC商业画布V1.0.md
商业论证总纲。用 OPC/PBA/VPC/NPDP/BMC 五张画布铺完：用户画像（30-60 岁父母，尤其母亲）、四类痛点（P0 故事丢失 / P1 写不出 / P2 难坚持 / P3 通用 AI 太空）、价值主张、三档定价（免费→19→39 元/月 + AI 额度包 + 后续实体书）、V1-V3 路线、30 天行动清单、风险预判。**它是后面所有设计文档的「甲方需求」来源。**

### 2. 时光书H5 Demo功能实现文档.md
技术实现总纲。定义 Demo 目标（两天可演示）、权威信息架构（三栏）、必做/暂不做清单、三条核心用户流程（手动记录/聊天整理/生成内容）、页面字段、时间日志整理与三层去重、5 个生成工作流接口占位（TS 签名）、数据结构 JSON、两天排期、10 条验收标准。**它是把商业需求翻译成「能写代码」的规格书。**

### 3. 时光书H5 Demo前端设计文档.md
视觉与组件规范。定义视觉方向（亲子手账+轻量 AI 工作台）、色彩 Token、字体/控件规则、16 个核心组件、8 个页面元素、交互状态、8 条禁止事项（不把生成做成时间轴弹窗/不选单表单/我的页面不放生成入口等）。明确最终 UI 图片稿放在 `UI图片稿/`，并逐张标注「哪些是可变测试占位、哪些结构基准必须保留」。**它是开发者的视觉宪法。**

### 4. 时光书H5 Demo用户路径与交互逻辑文档.md
用户视角的行为地图。用 8 个小目的（A-H：保存回忆/AI整理/进生成首页/带素材生成/生成文章/漫画/日记卡/看历史）+ 全局数据 + 完整业务逻辑表 + 6 类验收场景，把「用户点哪里、页面怎么变、调哪个工作流/数据」全部串起来。**它确保前端设计与用户真实操作不脱节。**

### 5. 时光书H5 Demo对抗性审查与修正建议.md
找茬文档。站在「用户会怎么误用」视角，列出 5 类风险（信息架构/时间轴/生成页/数据关系/UI），每条带 P0-P2 等级和修正方案。关键修正：生成 Tab 必须醒目、生成页主体必须是聊天、无来源素材时要提示「可直接聊天补充」、生成后本页预览不跳走、保存后我的页面必须有结果。**它把实现时最容易跑偏的坑先排了。**

### 6. 时光书H5 Demo页面UI设计提示词.md
11 条生图提示词（首页时间轴/新增回忆/AI整理聊天/草稿确认/生成首页/文章/漫画/日记卡/我的/页内预览）。每条自带产品说明、信息架构、视觉要求、禁止项，可直接粘贴给网页版生图工具。对应产出了 `UI图片稿/` 的 12 张图。**它是把设计文档转成视觉稿的「生产指令」。**

### 7. 时光书H5 Demo资产版规划与生图提示词.md
插画资产规划。先提取 `风格参考/` 4 张原图（猫咪健康主题）的版式/配色/Q 版语言，再要求**重构为亲子记忆主题**（去猫娘/咖啡/睡眠/实验室）。给出两张资产板生图提示词：①角色与插画资产版（父母/孩子/小精灵/表情/日常/道具/装饰）②背景与装饰资产版（渐变/纸纹/场景/天空/容器/分隔线）。**它是所有 UI 与角色一致性的源头。**

### 8. 时光书调研规划十条.md
证据→规划的合成文档（此前我生成）。把 OPC 画布（甲方需求）与墙内证据扫描（44 条真实帖）收敛成 10 条产品规划：具体用户、三组搜索词、证据、反复场景/反例/缺失、需求结论、产品边界、Agent 检查、主平台、账号人格、第一条内容。诚实标注两处与模板偏差（不记链接、44 非 30、评论区批量回填待补）。**它是商业论证与真实用户事实之间的桥。**

---

## 六、我的看法（整体评价 + 风险 + 建议）

### 整体评价：这是一套「设计完成度很高、但代码还没开始」的项目
八份文档环环相扣，从商业论证（画布）→ 技术规格（功能文档）→ 视觉宪法（前端文档）→ 用户行为（路径文档）→ 风险预案（对抗审查）→ 生产指令（UI/资产提示词），逻辑闭环、没有互相打架。尤其是三栏隔离 + 「生成页是聊天不是表单」+「本页预览不跳走」这三条，是反复被强调、也被对抗审查守住的铁律，说明你对产品意图想得很清楚。

### 几个我认为真实存在的风险
1. **代码为零**：所有文档都是设计/规格，H5 Demo 实际上**还没写一行**。两天排期也因先做证据采集而没动。下一步必须落到代码。
2. **前端框架未定**：文档里只出现 TS 函数签名（`async function`），没说用 React 还是 Vue，也没说构建工具。先拍板，否则组件无法落地。
3. **图片上传链路有断点**：Demo 用本地 blob，但真实 Coze 要 URL。谁负责上传、传到哪（对象存储？）、URL 怎么回传，文档没写死，联调时会卡。
4. **Coze 云端依赖是硬约束**：叙事聊天走云端工作流，实时性、稳定性、密钥管理都要在真实接入时验证；Demo 用 mock 没问题，但路演/上线前要压测。
5. **内容预期管理**：漫画「不承诺真人还原」已写进文档，但真实用户可能仍期待「像我家孩子」。需要在文案和首屏持续强调「氛围插画」。
6. **证据缺口**：付费意愿类证据尚未采集（画布 PBA 早验第 4 问缺墙内支撑），宝宝树 403、抖音字段不全。要讲商业化，这块得补。

### 给知夏的具体建议（按优先级）
1. **立刻开搭 Demo 骨架**：设计已齐，按功能文档两天排期直接写。框架先定（建议 React + Vite + TS，或你熟悉的栈），localStorage 跑通，mock 全开。
2. **写死图片上传链路**：在「真实接入」章节补一段——上传到对象存储拿 URL，再塞进工作流入参。
3. **先交「第一条内容」**：按规划十条 ⑩，用「宝宝第一次叫妈妈」种子，把「照片+一句话→30 秒日记卡」做成可演示 Demo 第一屏，这是最能打动人的钩子。
4. **补证据短板**：用已沉淀的 `xhs-comment-extractor` skill 把表1 其余高赞帖评论区批量回填；补一条「付费意愿」检索词证据，让商业化论证闭合。
5. **守住三铁律**：实现时把「生成页聊天非表单 / 本页预览不跳走 / 我的页面无生成入口」做成代码级约束或组件封装，别在赶工时跑偏。

---

> 结论：商业想清楚、设计想清楚、风险想清楚，现在是「从纸到码」的执行关口。先把 Demo 骨架跑起来，比再补文档更有杠杆。

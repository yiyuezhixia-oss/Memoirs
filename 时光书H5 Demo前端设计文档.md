# 时光书 H5 Demo 前端设计文档

## 1. 设计目标

时光书是面向父母的亲子回忆 H5 工具。设计目标是让用户清楚理解三件事：

1. `时间轴` 用来沉淀孩子成长回忆。
2. `生成` 是独立创作区，包含 AI整理回忆、文章、漫画、日记卡。
3. `我的` 只查看已保存历史作品。

整体视觉采用“亲子手账 + 轻量 AI 工作台”：温柔、治愈、有相册和书页感，但不幼稚。

## 2. 权威信息架构

底部主导航固定为：

`时间轴 / 生成 / 我的`

| 页面 | 路由建议 | 职责 |
| --- | --- | --- |
| 时间轴首页 | `/` | 回忆日志沉淀、浏览、新增、AI 整理快捷入口。 |
| 新增回忆页 | `/entry/new` | 手动上传图片、写文字、选择日期粒度。 |
| 记忆整理聊天页 | `/memory-chat` | 通过聊天整理时间轴日志。 |
| 日志草稿确认弹窗 | 弹窗 | AI 整理后确认写入时间轴。 |
| 生成首页 | `/generate` | AI整理回忆、文章、漫画、日记卡四个入口。 |
| 文章生成页 | `/generate/article` | 多轮聊天确认文章想法，本页生成预览。 |
| 漫画生成页 | `/generate/comic` | 多轮聊天确认画面和参考图，本页生成预览。 |
| 日记卡生成页 | `/generate/diary-card` | 多轮聊天确认主图、标题、短句，本页生成预览。 |
| 我的页面 | `/mine` | 只查看已保存作品历史，按三类分类。 |

关键规则：

1. 生成页不是时间轴的附属流程。
2. 时间轴卡片可以点击「用于生成」，但只是把素材带入对应生成工作台。
3. 三个生成页主体都是多轮聊天，不是选择题表单。
4. 生成完成后在当前生成页内切换预览状态，不跳独立预览页。
5. 我的页面不提供生成入口。

## 3. 视觉系统

### 3.1 视觉方向

参考用户生成的 `角色与插画资产版`、`背景与装饰资产版`，以及已有 UI 组件参考：

1. 柔和粉白、浅紫、奶油黄、薄荷绿、浅蓝。
2. Q 版亲子插画、时光书小精灵、相册、书页、便签、相纸。
3. 软圆角卡片、浅阴影、粉色主按钮、纸纹背景。
4. 装饰只做点缀：小花、星星、胶带、相纸角标、虚线分隔。

### 3.2 色彩 Token

```css
:root {
  --bg: #F8F4EC;
  --paper: #FFFDF8;
  --paper-soft: #FBF1E5;
  --ink: #2F2A26;
  --muted: #7A7067;
  --line: #E7D8C8;
  --rose: #D97878;
  --sage: #7A9B83;
  --sky: #6E91B8;
  --gold: #C89A4B;
  --danger: #C35B4A;
  --radius-sm: 6px;
  --radius-md: 8px;
  --shadow-paper: 0 8px 24px rgba(89, 63, 38, 0.08);
  --shadow-float: 0 10px 30px rgba(89, 63, 38, 0.16);
}
```

### 3.3 字体与控件

| 类型 | 移动端字号 |
| --- | --- |
| 页面标题 | 20px / 600 |
| 卡片标题 | 18px / 600 |
| 正文 | 15px / 400 |
| 辅助说明 | 12px / 400 |
| 按钮 | 15px / 500 |

控件规则：

1. 主按钮：粉色或深墨色实心，8px 圆角。
2. 次按钮：白底描边，8px 圆角。
3. 卡片：纸白底、细线、轻阴影、8px 圆角。
4. 输入框：便签纸或纸白底，focus 使用浅蓝边框。
5. 底部导航：三个固定项，当前项粉色高亮。

## 4. 核心组件

| 组件 | 用途 |
| --- | --- |
| `AppShell` | 移动端容器，最大宽度 430px，桌面居中。 |
| `TopBar` | 页面标题、返回、右侧动作。 |
| `BottomNav` | 时间轴 / 生成 / 我的。 |
| `TimelineCard` | 时间轴日志卡片。 |
| `FloatingAddButton` | 时间轴新增入口。 |
| `DatePrecisionPicker` | 年、年月、日期、区间。 |
| `ImageUploader` | 图片上传和本地预览。 |
| `MemoryChatPanel` | AI 整理回忆聊天。 |
| `DraftConfirmSheet` | 日志草稿确认弹窗。 |
| `GenerationHomeCard` | 生成首页四个入口卡片。 |
| `GenerationWorkspace` | 三个生成页共用工作台框架。 |
| `GenerationChatPanel` | 生成页内部多轮聊天。 |
| `SourceMemoryCard` | 生成页可选来源素材卡。 |
| `InlinePreviewPanel` | 生成页内预览状态。 |
| `MyWorksTabs` | 我的页面文章/漫画/日记卡分类。 |
| `WorkCard` | 历史作品卡片。 |

## 5. 页面设计

### 5.1 时间轴首页

职责：展示回忆日志，不承载生成流程本体。

页面元素：

1. 顶部「时光书」和「AI整理」。
2. 孩子档案摘要卡：头像/亲子插画、`安安的时光书`、记录数量、`新增回忆`。
3. 时间轴列表：日期倒序，每条是书页卡片。
4. 卡片底部按钮：`用于生成`，仅用于带素材进入生成工作台。
5. 右下角 `+` 新增按钮。
6. 底部导航选中 `时间轴`。

### 5.2 新增回忆页

职责：手动创建时间轴日志。

页面元素：

1. 返回 + 标题 `新增回忆`。
2. 提示卡：`记不清具体日期也没关系`。
3. 日期粒度：年、年月、日期、区间。
4. 图片上传区。
5. 文字输入区。
6. 场景 chip。
7. 底部按钮 `保存到时间轴`。

### 5.3 记忆整理聊天页

职责：聊天整理为时间轴日志。

页面元素：

1. 标题 `AI整理回忆`。
2. 采访说明卡。
3. 聊天气泡：AI 追问和用户回答。
4. 图片上传、文本输入、发送。
5. 底部按钮 `整理 N 条新回答`。
6. 整理完成后弹出 `DraftConfirmSheet`。

### 5.4 生成首页

职责：独立创作区入口，包含 AI整理回忆和三个生成工作台。

页面元素：

1. 标题 `生成`。
2. 插画横幅：`把一段回忆变成一件作品`。
3. 四个入口卡：
   - `AI整理回忆`
   - `生成文章`
   - `生成漫画`
   - `生成日记卡`
4. 最近创作草稿。
5. 底部导航选中 `生成`。

### 5.5 文章生成页

职责：通过多轮聊天生成文章。

页面元素：

1. 标题 `生成文章`。
2. 可选 `已带入回忆` 素材卡；没有来源时显示可直接聊天补素材。
3. 说明卡：确认写给谁、用途、想表达的话、文风。
4. 多轮聊天区。
5. 输入区。
6. 底部按钮 `确认生成文章`。
7. 生成后同页切换文章预览：书页正文、`继续补充想法并重生成`、`保存作品`。

### 5.6 漫画生成页

职责：通过多轮聊天生成亲子氛围漫画。

页面元素：

1. 标题 `生成漫画`。
2. 可选来源素材卡。
3. 参考图相纸区。
4. 提示：`不承诺真人还原，会生成亲子氛围插画`。
5. 多轮聊天区：画面、动作、表情、情绪。
6. 底部按钮 `确认生成漫画`。
7. 生成后同页切换漫画预览。

### 5.7 日记卡生成页

职责：通过多轮聊天生成一张可分享日记卡。

页面元素：

1. 标题 `生成日记卡`。
2. 可选来源素材卡。
3. 日记卡草稿骨架。
4. 主图确认区。
5. 多轮聊天区：标题、短句、氛围。
6. 底部按钮 `确认生成日记卡`。
7. 生成后同页切换竖版日记卡预览。

### 5.8 我的页面

职责：只查看历史作品。

页面元素：

1. 标题 `我的`。
2. 用户摘要卡：`安安的作品集`、已保存作品数。
3. 分类 Tab：文章、漫画、日记卡。
4. 作品卡：缩略图、标题、生成时间、关联回忆、`查看`。
5. 空分类提示：`去生成页创作后会出现在这里`。
6. 不出现新的生成主按钮。
7. 底部导航选中 `我的`。

## 6. 最终 UI 图片稿

最终确认的页面图片稿统一放在：

`D:\ai_biancheng\时光书\UI图片稿`

这些图片是 Demo 第一版的前端视觉和页面结构基准。开发时以页面结构、布局层级、导航关系、按钮位置、视觉风格为准；图片中的具体生成内容、作品标题、数量、日期、聊天文案、缩略图和产出结果属于测试占位，后续接入真实工作流和测试数据后会替换。

| 文件 | 对应页面/状态 | 是否作为结构基准 | 占位或后续会变更内容 |
| --- | --- | --- | --- |
| `01-时间轴首页.png` | 时间轴首页 | 是 | 孩子姓名、已记录数量、时间轴日志标题、照片、日志正文、日期、标签均为测试内容；真实数据来自时间轴记录。 |
| `02-新增回忆页.png` | 新增回忆页 | 是 | 示例照片、默认日期、场景标签、输入框提示为测试示例；实际上传图、日期粒度、标签由用户操作产生。 |
| `03-AI整理回忆聊天页.png` | AI整理回忆聊天状态 | 是 | 聊天内容、已读时间、整理条数是测试文案；实际由 AI 整理工作流和用户输入生成。 |
| `04-AI整理回忆草稿确认.png` | AI整理后草稿确认弹层 | 是 | 推测日期、标题、正文、照片排序是测试草稿；实际允许用户编辑后再写入时间轴。 |
| `05-生成首页.png` | 生成首页 | 是 | 最近创作列表、作品标题、轮次、缩略图是测试数据；四个入口结构固定：AI整理回忆、生成文章、生成漫画、生成日记卡。 |
| `06-文章生成聊天页.png` | 文章生成页聊天状态 | 是 | 聊天问题、用户回答、带入回忆内容是测试样例；真实流程由文章生成工作流引导。 |
| `07-文章生成预览状态.png` | 文章生成页本页预览状态 | 是 | 文章标题、正文、装饰文案是测试产物；真实内容由生成结果替换。 |
| `08-漫画生成聊天页.png` | 漫画生成页聊天状态 | 是 | 风格参考图、分镜数量、聊天内容是测试样例；真实素材和选择由用户上传或聊天确认。 |
| `09-漫画生成预览状态.png` | 漫画生成页本页预览状态 | 是 | 6 格漫画画面、对白、风格参数是测试产物；真实内容由漫画生成工作流替换。 |
| `10-日记卡生成聊天页.png` | 日记卡生成页聊天状态 | 是 | 聊天内容、已带入回忆、版式选项是测试样例；真实流程由日记卡工作流引导。 |
| `11-日记卡生成预览状态.png` | 日记卡生成页本页预览状态 | 是 | 日记卡成品图、标题、短句、天气地点心情为测试产物；实际成品由生成结果替换。 |
| `12-我的页面.png` | 我的页面 | 是 | 头像、昵称、等级、作品数量、作品列表为测试数据；页面只保留文章、漫画、日记卡分类浏览，不作为生成入口。 |

### 6.1 图片稿实现边界

必须保留：

1. 底部主导航固定为 `时间轴 / 生成 / 我的`。
2. `生成首页` 固定为四个入口：AI整理回忆、生成文章、生成漫画、生成日记卡。
3. 三个生成页都以多轮聊天为主体，选择项只作为聊天中的快捷补充。
4. 生成完成后在当前生成页切换为预览状态。
5. `我的页面` 只查看已保存作品，不启动新的生成流程。

可替换或可调整：

1. 所有测试人物名、孩子名、作品名、日期、数量。
2. 所有生成结果内容，包括文章正文、漫画格子图、日记卡图文。
3. 时间轴与我的页面中的缩略图。
4. 聊天气泡里的示例问答。
5. 图标可换成前端实际图标库，但语义和位置应保持一致。
6. 生成页的“创作中”状态文案可根据真实接口状态细化。

### 6.2 需要避免误用的占位信息

1. 图片中的 `9:41` 是设计稿状态栏占位，不进入 H5 真实页面。
2. 图片中的手机信号、电量状态栏是视觉稿占位，H5 实现不需要绘制。
3. 图片中的具体作品数量不是业务初始值，真实环境应由接口返回。
4. 图片中的测试图不是固定素材库，真实 Demo 可使用上传图片、预置测试素材或生成结果。
5. 图片中的“已读”“09:30”等聊天时间是示意，Demo 可先不实现已读状态。
6. 图片中的“LV.3”属于个人页氛围占位，Demo 可先静态展示或暂不接真实等级体系。

## 7. 交互状态

### 7.1 生成页状态

| 状态 | 展示 |
| --- | --- |
| 聊天采集中 | 多轮聊天 + 输入区 + 确认生成按钮。 |
| 生成中 | 当前页 loading，文案如“正在把这段回忆变成作品”。 |
| 预览 | 当前页展示成品，保留继续补充和保存作品。 |
| 失败 | 当前页显示重试按钮，不跳走。 |

### 7.2 来源素材

1. 从时间轴进入生成页时，展示 `已带入回忆`。
2. 从生成首页直接进入时，展示 `还没有带入素材，可以直接聊天补充`。
3. 来源素材不是必填。

## 8. 禁止事项

1. 不把生成流程设计成时间轴附属弹窗。
2. 不做独立生成预览页作为主要流程。
3. 不把三个生成页做成选择题表单。
4. 不在我的页面放生成入口。
5. 不使用猫娘、猫咖、咖啡、睡眠、实验室主题。
6. 不使用过度贴纸化的儿童模板风。
7. 不承诺漫画真人还原。

## 9. Demo 验收标准

1. 底部导航清晰显示 `时间轴 / 生成 / 我的`。
2. 时间轴能新增和 AI 整理回忆。
3. 生成首页能进入 AI整理回忆、文章、漫画、日记卡四个入口。
4. 三个生成页都以多轮聊天为主体。
5. 从生成首页进入生成页时，不需要先选时间轴日志。
6. 从时间轴 `用于生成` 进入时，生成页能显示已带入素材。
7. 生成完成后在当前生成页展示预览。
8. 我的页面只展示历史作品，不提供创作入口。

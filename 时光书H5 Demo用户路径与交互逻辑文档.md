# 时光书 H5 Demo 用户路径与交互逻辑文档

## 1. 权威信息架构

H5 Demo 使用三大主区，底部主导航固定为：

`时间轴 / 生成 / 我的`

| 主区 | 职责 | 不做什么 |
| --- | --- | --- |
| 时间轴 | 回忆日志沉淀、浏览、新增、AI 整理快捷入口。 | 不承载生成流程本体。 |
| 生成 | 独立创作区，包含 AI整理回忆、文章、漫画、日记卡四个入口。 | 不只是时间轴卡片的附属弹窗。 |
| 我的 | 查看已保存历史作品，按文章、漫画、日记卡分类。 | 不作为生成入口。 |

生成素材来源是可选的：

1. 用户可以从时间轴某条日志点击“用于生成”，把该日志作为素材带入生成工作台。
2. 用户也可以直接从 `生成` Tab 进入工作台，通过聊天补充全部素材。

生成完成后不跳独立预览页，而是在当前生成页内切换为预览状态。

## 2. 全局数据

| 数据 | 用途 |
| --- | --- |
| `timelineEntries` | 时间轴日志。 |
| `memoryChatMessages` | AI 整理回忆聊天记录。 |
| `generationTasks` | 文章、漫画、日记卡生成任务。 |
| `generationTasks.chatMessages` | 每个生成任务内部的多轮聊天记录。 |
| `generationTasks.sourceEntryId` | 可选，来自时间轴的素材来源。 |
| `generationTasks.result` | 生成后的文章、漫画或日记卡结果。 |
| `timelineEntry.relatedGenerationIds` | 第二阶段预留，用于把生成作品反向关联时间轴。 |

Demo 第一阶段：生成页聊天只需要保存到生成作品。  
Demo 第二阶段：再支持把生成页聊天整理归档到时间轴。

## 3. 用户刚进入

用户默认进入 `时间轴` Tab。

用户看到：

1. 顶部「时光书」。
2. 孩子档案摘要。
3. 时间轴日志列表。
4. 右下角新增按钮。
5. 底部导航：时间轴、生成、我的。

用户可能点击：

| 点击 | 跳转/效果 |
| --- | --- |
| 新增回忆 | 进入新增回忆页。 |
| AI整理 | 进入记忆整理聊天页，这是生成页 AI整理回忆入口的快捷方式。 |
| 用于生成 | 选择文章/漫画/日记卡后，带当前日志进入对应生成页。 |
| 底部生成 | 进入生成首页。 |
| 底部我的 | 进入我的页面。 |

## 4. 小目的 A：保存一条时间轴回忆

入口：

1. 时间轴右下角 `+`。
2. 时间轴孩子摘要卡内「新增回忆」。

流程：

1. 用户进入新增回忆页。
2. 选择日期粒度：年、年月、日期、区间。
3. 上传图片或使用示例图。
4. 输入文字。
5. 点击「保存到时间轴」。
6. 成功后返回时间轴，新日志按日期倒序出现。

前端逻辑：

| 按钮 | 前端动作 | 后端/工作流 |
| --- | --- | --- |
| 保存到时间轴 | 校验日期与内容，创建 timelineEntry。 | Demo 写 localStorage；正式接创建日志 API。 |

边界：

1. 图片和文字至少有一个。
2. 日期可以模糊，但不能完全无时间；可用“时间待补”作为第二阶段。
3. 连续点击保存时按钮 disabled。

## 5. 小目的 B：通过 AI 整理回忆到时间轴

入口：

1. 时间轴顶部「AI整理」快捷入口。
2. 生成首页「AI整理回忆」入口。

流程：

1. 进入记忆整理聊天页。
2. 用户发照片或讲述故事。
3. AI 追问时间、事件、孩子说的话、父母感受。
4. 用户点击「整理 N 条新回答」。
5. 系统生成日志草稿。
6. 用户在草稿弹窗编辑日期、标题、正文。
7. 点击「写入时间轴」。
8. 返回时间轴。

前端逻辑：

| 按钮 | 前端动作 | 后端/工作流 |
| --- | --- | --- |
| 发送 | 追加 pending 聊天消息。 | 叙事追问工作流或 mock。 |
| 整理 N 条新回答 | 提交 pending 消息。 | 时间日志整理工作流。 |
| 写入时间轴 | 创建 timelineEntry，消息标记 processed。 | 创建日志 API 或 localStorage。 |

去重规则：

1. 只整理 `pending` 用户消息。
2. 只有草稿成功写入时间轴后，消息才标记 `processed`。
3. 用户取消或失败时，消息恢复 `pending`。

## 6. 小目的 C：进入生成首页

入口：底部导航「生成」。

用户看到：

1. 顶部标题「生成」。
2. 插画横幅「把一段回忆变成一件作品」。
3. 四个入口：AI整理回忆、生成文章、生成漫画、生成日记卡。
4. 最近创作草稿。

用户点击：

| 点击 | 跳转 |
| --- | --- |
| AI整理回忆 | 进入记忆整理聊天页。 |
| 生成文章 | 进入文章生成页，无 sourceEntryId。 |
| 生成漫画 | 进入漫画生成页，无 sourceEntryId。 |
| 生成日记卡 | 进入日记卡生成页，无 sourceEntryId。 |

## 7. 小目的 D：从时间轴带素材去生成

入口：时间轴卡片「用于生成」。

流程：

1. 用户点击某条日志的「用于生成」。
2. 弹出轻量选择菜单：文章、漫画、日记卡。
3. 用户选择类型。
4. 进入对应生成页。
5. 页面显示「已带入回忆」素材卡。

注意：

这只是把时间轴日志带入生成工作台，不代表生成流程附属于时间轴。

## 8. 小目的 E：生成文章

入口：

1. 生成首页 -> 生成文章。
2. 时间轴日志 -> 用于生成 -> 文章。

文章生成页状态一：聊天创作中。

用户看到：

1. 顶部「生成文章」。
2. 可选素材卡：有 sourceEntryId 时显示已带入回忆；没有时显示可直接聊天补充。
3. 说明卡：「我会先和你聊聊想写给谁、想表达什么，再生成文章。」
4. 多轮聊天区。
5. 底部输入区。
6. 底部主按钮「确认生成文章」。

聊天重点：

1. 写给谁。
2. 使用场景。
3. 最想表达的话。
4. 文风参考。

点击「确认生成文章」：

1. 创建或更新 generationTask。
2. 提交当前 sourceEntryId 和 chatMessages。
3. 调用文章生成工作流。
4. 当前页进入生成中。
5. 成功后当前页切换为文章预览状态。

文章生成页状态二：本页预览。

用户看到：

1. 文章书页预览。
2. 输入条「还想调整哪里？」。
3. 按钮「补充并重生成」。
4. 按钮「保存作品」。

## 9. 小目的 F：生成漫画

入口：

1. 生成首页 -> 生成漫画。
2. 时间轴日志 -> 用于生成 -> 漫画。

漫画生成页聊天重点：

1. 想画哪一幕。
2. 人物动作。
3. 情绪氛围。
4. 是否使用参考图。

点击「确认生成漫画」后：

1. 调用漫画生成工作流。
2. 当前页进入生成中。
3. 成功后当前页切换为漫画预览状态。

重要提示：

页面必须显示“不承诺真人还原，会生成亲子氛围插画”。

## 10. 小目的 G：生成日记卡

入口：

1. 生成首页 -> 生成日记卡。
2. 时间轴日志 -> 用于生成 -> 日记卡。

日记卡生成页聊天重点：

1. 主图。
2. 标题。
3. 最想保留的一句话。
4. 卡片氛围。

点击「确认生成日记卡」后：

1. 调用日记卡生成工作流。
2. 当前页进入生成中。
3. 成功后当前页切换为日记卡预览状态。

## 11. 小目的 H：查看历史作品

入口：底部导航「我的」。

用户看到：

1. 顶部「我的」。
2. 用户摘要卡「安安的作品集」。
3. 分类 Tab：文章、漫画、日记卡。
4. 当前分类的作品列表。

用户点击：

| 点击 | 效果 |
| --- | --- |
| 文章 Tab | 查看文章历史。 |
| 漫画 Tab | 查看漫画历史。 |
| 日记卡 Tab | 查看日记卡历史。 |
| 查看 | 打开作品详情或对应生成页预览状态。 |

我的页面不提供新的生成入口。

## 12. 完整业务逻辑表

| 业务 | 入口 | 页面变化 | 工作流/数据 |
| --- | --- | --- | --- |
| 新增回忆 | 时间轴 `+` | 新增页 -> 时间轴 | 创建 timelineEntry。 |
| AI 整理回忆 | 时间轴 AI整理或生成首页 AI整理回忆 | 聊天 -> 草稿弹窗 -> 时间轴 | 整理 pending 消息为 timelineEntry。 |
| 进入生成首页 | 底部生成 | 时间轴/我的 -> 生成首页 | 展示四个入口。 |
| 直接生成文章 | 生成首页文章入口 | 文章生成页聊天 -> 本页预览 | generate-article。 |
| 直接生成漫画 | 生成首页漫画入口 | 漫画生成页聊天 -> 本页预览 | generate-comic。 |
| 直接生成日记卡 | 生成首页日记卡入口 | 日记卡生成页聊天 -> 本页预览 | generate-diary-card。 |
| 带日志生成 | 时间轴用于生成 | 选择类型 -> 对应生成页 | generationTask.sourceEntryId = entryId。 |
| 保存作品 | 生成页预览状态 | 当前页提示保存成功 | 保存 generationTask.result。 |
| 查看历史 | 我的分类 Tab | 我的页列表/详情 | 读取 generationTasks。 |

## 13. 验收场景

1. 从 `生成` Tab 进入文章生成页，不选择时间轴日志，直接聊天补素材，生成后本页预览。
2. 从时间轴某条日志点击「用于生成」，选择漫画，进入漫画生成页，显示已带入素材，继续聊天后本页预览。
3. 日记卡生成页通过聊天确认短句，生成后同页展示竖版日记卡。
4. 我的页面只展示历史作品，不出现生成入口。
5. 从生成首页点击 AI整理回忆，也能进入同一套记忆整理聊天流程。
6. AI 整理回忆聊天写入时间轴后，重复点击不会重复归档。

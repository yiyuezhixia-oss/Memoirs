# 时光书 H5 Demo 页面 UI 设计提示词

## 1. 统一使用说明

在网页版生成 UI 图时，先手动上传这些参考图：

1. 用户生成的 `01 角色与插画资产版`。
2. 用户生成的 `02 背景与装饰资产版`。
3. UI 组件参考版。
4. UI 页面参考版。

网页版没有本地上下文，所以每条提示词都已经包含产品说明、页面目标、页面结构和禁忌。

统一产品说明：

「时光书」是一款面向父母的亲子回忆 H5 工具。父母可以上传孩子照片、通过聊天讲述故事、写下感受，系统把内容沉淀为孩子成长时间轴，并进一步生成文章、漫画和日记卡。

统一信息架构：

1. 底部主导航固定为 `时间轴 / 生成 / 我的`。
2. `时间轴` 只负责回忆日志沉淀、浏览、新增和 AI整理快捷入口。
3. `生成` 是独立创作区，包含 AI整理回忆、文章生成、漫画生成、日记卡生成四个入口。
4. 文章、漫画、日记卡三个生成页都支持多轮聊天确认想法，不是选择题表单。
5. 生成素材来源可选：可以从时间轴带入，也可以在生成页直接聊天采集。
6. 生成完成后在当前生成页内切换为预览状态，不跳独立预览页。
7. `我的` 只查看历史作品，不作为生成入口。

统一视觉要求：

1. 参考资产版的柔和粉白、浅紫、奶油黄、薄荷绿、浅蓝色系。
2. 参考 UI 组件版的软圆角卡片、粉色主按钮、浅阴影、输入框、chip、底部按钮。
3. 参考 UI 页面版的移动端 H5 布局、顶部栏、大插画头图、卡片区和表单区。
4. 使用中文界面文案。
5. 页面尺寸为手机竖屏，推荐 390x844 或 430x932。
6. 不要猫娘、猫咖、咖啡、睡眠、实验室主题。

## 2. 首页时间轴

```text
我正在为「时光书」生成移动端 H5 UI 页面设计稿。

产品说明：
「时光书」是一款面向父母的亲子回忆 H5 工具。父母可以上传孩子照片、通过聊天讲述故事、写下感受，系统把内容沉淀为孩子成长时间轴，并进一步生成文章、漫画和日记卡。

请参考我上传的角色与插画资产版、背景与装饰资产版、UI 组件参考版和 UI 页面参考版。参考它们的柔和粉白紫配色、软圆角卡片、轻水彩插画、相册/书页/便签元素、粉色主按钮、温柔亲子视觉。不要复制猫娘、猫咖、睡眠、咖啡、实验室主题。

页面名称：
「首页时间轴」

页面目标：
用户进入后看到孩子的回忆时间轴。这个页面只负责回忆日志沉淀与浏览，不承载生成流程本体。

页面结构：
1. 顶部导航栏：
   - 左侧产品名「时光书」。
   - 右侧按钮「AI整理」，这是进入生成页 AI整理回忆能力的快捷入口。

2. 孩子档案摘要卡：
   - 左侧亲子小插画或孩子头像。
   - 标题「安安的时光书」。
   - 副文案「把今天留给以后」。
   - 数据「已记录 12 段回忆」。
   - 文字按钮「新增回忆」。

3. 时间轴列表：
   - 按日期倒序。
   - 每条日志是书页卡片。
   - 卡片包含日期书签、照片、标题、正文摘要、来源标记。
   - 示例一：日期「今天」，标题「第一次认真说谢谢」。
   - 示例二：日期「2026年6月」，标题「毕业那天的拥抱」。

4. 时间轴卡片底部：
   - 次级按钮「用于生成」。
   - 点击后只是把这条回忆作为素材带入生成工作台，不是直接生成。

5. 底部主导航：
   - 三项：「时间轴」「生成」「我的」。
   - 当前选中「时间轴」。

6. 右下角悬浮加号按钮：
   - 文案气泡「新增」。

视觉要求：
背景使用「回忆书页」或清晨粉白纸纹。时间轴用细虚线、花朵分隔、相纸角标。卡片温柔清晰，不要营销首页感。

禁止：
不要把文章/漫画/日记卡三个生成入口放成首页主功能。不要做成营销 hero。不要猫、咖啡、睡眠主题。
```

## 3. 新增回忆页

```text
为「时光书」生成移动端 H5 UI 页面设计稿。

产品说明：
「时光书」帮助父母把照片、文字和聊天沉淀为孩子成长时间轴。

页面名称：
「新增回忆」

页面目标：
用户手动新增一条时间日志，支持只选年、只选年月、具体日期、日期区间，上传图片并写文字。

页面结构：
1. 顶部导航栏：
   - 返回箭头。
   - 标题「新增回忆」。

2. 顶部提示卡：
   - 时光书小精灵插画。
   - 文案「记不清具体日期也没关系，先把这一刻留下来。」

3. 日期精度选择：
   - 分段控件：「年」「年月」「日期」「区间」。
   - 默认选中「日期」。
   - 日期输入示例「2026.08.05」。

4. 图片上传区：
   - 标题「照片」。
   - 大相纸上传框。
   - 已选图片以相纸缩略图展示。

5. 文字输入区：
   - 标题「这段回忆」。
   - 便签纸风格多行输入框。
   - placeholder「写下当时发生了什么，或者你现在想对孩子说的话。」

6. 场景 chip：
   - 「日常」「生日」「毕业」「旅行」「第一次」。

7. 底部固定主按钮：
   - 「保存到时间轴」。

视觉要求：
像亲子手账的一页。使用相册纸、便签纸、小花、星星、轻纸纹。表单清晰，不要装饰过量。

禁止：
不要后台表单风，不要选择题堆满屏，不要猫咖/睡眠元素。
```

## 4. 记忆整理聊天页

```text
为「时光书」生成移动端 H5 UI 页面设计稿。

产品说明：
「时光书」可以通过聊天帮助父母把零散回忆整理成时间轴日志。

页面名称：
「AI整理回忆」

页面目标：
用户通过多轮聊天讲述亲子回忆，AI 追问时间、事件、孩子说的话和父母感受，最后整理成时间轴日志草稿。

页面结构：
1. 顶部导航栏：
   - 返回箭头。
   - 标题「AI整理回忆」。
   - 右侧小字「仅本次整理」。

2. 采访说明卡：
   - 时光书小精灵插画。
   - 文案「慢慢说，我会帮你整理成一页时光书。」
   - 副文案「可以发照片，也可以先讲故事。」

3. 聊天消息区：
   - AI 气泡在左侧，纸白色卡片风。
   - 用户气泡在右侧，浅粉或浅蓝。
   - 示例 AI：「这是谁的哪一段回忆？你可以先发一张照片，或者直接告诉我发生了什么。」
   - 示例用户：「这是孩子毕业那天，他跑过来抱了我一下。」
   - 示例 AI：「那一刻你心里最强烈的感受是什么？」

4. 底部输入区：
   - 图片按钮。
   - 输入框 placeholder「慢慢说，细节越小越珍贵。」
   - 发送按钮。

5. 底部固定主按钮：
   - 「整理 2 条新回答」。

视觉要求：
像温柔采访，不像客服聊天，也不像微信截图。背景使用纸纹、窗边书桌或相册桌面元素。

禁止：
不要让它像娱乐聊天机器人。页面目的必须明显是整理回忆到时间轴。
```

## 5. 日志草稿确认弹窗

```text
为「时光书」生成移动端 H5 UI 设计稿，重点展示一个底部弹窗。

页面名称：
「日志草稿确认」

页面目标：
AI 根据聊天整理出时间日志草稿，用户确认或修改后写入时间轴。

画面结构：
背景是半透明的聊天页，前景为底部圆角弹窗。

弹窗内容：
1. 标题「整理好了，看看像不像这段回忆」。
2. 提示「日期和文字都可以修改」。
3. 日期编辑区：「2026年6月」，小标签「AI推测」。
4. 标题输入：「毕业那天的拥抱」。
5. 正文编辑区：
   「他从人群里跑过来抱住我，我开心，也有一点舍不得。那一刻，我突然觉得他真的长大了。」
6. 图片预览：1-2 张相纸缩略图。
7. 折叠行：「查看原始聊天」。
8. 底部按钮：
   - 次按钮「返回修改」。
   - 主按钮「写入时间轴」。

视觉要求：
弹窗像从时光书里抽出的纸页，浅阴影、柔和边缘、粉色主按钮。

禁止：
不要系统 alert 风，不要让草稿看起来不可编辑。
```

## 6. 生成首页

```text
为「时光书」生成移动端 H5 UI 页面设计稿。

产品说明：
「时光书」的生成区是独立创作区，不依附于时间轴。用户可以选择 AI整理回忆、文章、漫画、日记卡四个入口。AI整理回忆用于把聊天沉淀为时间轴日志；文章、漫画、日记卡进入后通过聊天补充素材和确认想法，再生成预览。

页面名称：
「生成首页」

页面目标：
展示四个入口：AI整理回忆、文章生成、漫画生成、日记卡生成。这里不是历史页，也不是时间轴。

页面结构：
1. 顶部导航栏：
   - 标题「生成」。
   - 副按钮「作品说明」或小问号图标。

2. 顶部插画横幅：
   - 父母记录者、孩子、时光书小精灵围着一本打开的时光书。
   - 文案「把一段回忆变成一件作品」。
   - 副文案「可以先带入时间轴回忆，也可以直接聊天补充素材。」

3. 四个入口卡片：
   - 「AI整理回忆」
     说明「和我聊聊，把故事整理进时间轴」。
     图标或插画：时光书小精灵、打开的相册、聊天气泡。
   - 「生成文章」
     说明「适合生日、毕业、写给孩子的一封信」。
     图标或插画：打开的书、信纸、笔。
   - 「生成漫画」
     说明「通过聊天确认想画哪一幕」。
     图标或插画：漫画格、画笔、相纸。
   - 「生成日记卡」
     说明「适合保存和分享一页回忆」。
     图标或插画：竖版卡片、照片、星星。

4. 最近草稿区：
   - 小标题「最近创作」。
   - 展示 1-2 条未完成或最近生成的作品卡。

5. 底部主导航：
   - 「时间轴」「生成」「我的」。
   - 当前选中「生成」。

视觉要求：
生成首页要像创作工作台入口，温柔但有行动感。四个入口卡片要足够大、清晰、有插画。AI整理回忆可以作为第一张主入口卡，文章/漫画/日记卡作为三个创作入口。

禁止：
不要做成从时间轴弹出的菜单。不要把它做成我的作品历史页。不要选择题表单。
```

## 7. 文章生成页

```text
为「时光书」生成移动端 H5 UI 页面设计稿。

产品说明：
「时光书」文章生成页是独立工作台。用户可以从时间轴带入一条回忆，也可以不带入，直接通过多轮聊天补充素材。页面主体是聊天，不是选择题表单。生成完成后在本页切换为文章预览状态。

页面名称：
「文章生成页」

页面目标：
通过聊天确认写给谁、用途、想表达的话、文风，然后生成文章并在本页预览。

页面结构：
1. 顶部导航栏：
   - 返回箭头。
   - 标题「生成文章」。
   - 右侧小字「创作中」。

2. 可选素材卡：
   - 如果已从时间轴带入，显示「已带入回忆」。
   - 示例标题「毕业那天的拥抱」。
   - 显示日期、相纸缩略图、摘要。
   - 也可以显示小按钮「更换素材」。

3. 生成说明卡：
   - 使用生成文章小插画。
   - 文案「我会先和你聊聊想写给谁、想表达什么，再生成文章。」

4. 多轮聊天区：
   - AI：「这篇文章想写给孩子本人，还是给家人一起看？」
   - 用户：「写给孩子本人。」
   - AI：「你最想让他记住哪句话？」
   - 用户：「妈妈一直为你骄傲，也会慢慢学着放手。」
   - AI：「文风想更像温柔家书，还是故事散文？」

5. 底部输入区：
   - placeholder「补充你想表达的话」。
   - 发送按钮。

6. 底部固定主按钮：
   - 「确认生成文章」。

7. 本页预览状态：
   - 生成后同一页面切换为文章预览。
   - 顶部仍显示「生成文章」。
   - 中央是书页样式文章，标题「写给毕业那天的你」。
   - 底部保留「继续补充想法并重新生成」「保存作品」。

视觉要求：
页面像温柔写作工作台。聊天气泡、信纸、书页、花朵、小精灵装饰。不要像普通 IM。

禁止：
不要用途选择题卡片，不要下拉框，不要独立预览页，不要支付信息。
```

## 8. 漫画生成页

```text
为「时光书」生成移动端 H5 UI 页面设计稿。

产品说明：
「时光书」漫画生成页是独立工作台。用户可以从时间轴带入回忆，也可以直接聊天讲述画面。页面主体是多轮聊天，确认想画哪一幕、人物动作、情绪和参考图。生成完成后在本页切换为漫画预览状态。

页面名称：
「漫画生成页」

页面目标：
通过聊天确认画面重点，再生成亲子氛围漫画。

页面结构：
1. 顶部导航栏：
   - 返回箭头。
   - 标题「生成漫画」。
   - 右侧小字「先聊画面」。

2. 可选素材卡：
   - 显示「已带入回忆」或「还没有带入素材」。
   - 如果有素材，展示日期、标题、相纸图。

3. 参考图区域：
   - 相纸框显示当前参考图。
   - 小按钮「上传参考图」或「换参考图」。
   - 小提示「不承诺真人还原，会生成亲子氛围插画」。

4. 多轮聊天区：
   - AI：「这段回忆里，你最想变成漫画的是哪一幕？」
   - 用户：「他抱着毕业证跑过来抱我。」
   - AI：「画面里妈妈是什么表情？开心、感动，还是有点舍不得？」
   - 用户：「开心，但眼眶有点湿。」

5. 底部输入区：
   - placeholder「描述画面、动作或情绪」。
   - 图片按钮、发送按钮。

6. 底部固定主按钮：
   - 「确认生成漫画」。

7. 本页预览状态：
   - 生成后同页展示漫画预览。
   - 预览区像一张放在书桌上的漫画卡。
   - 底部按钮「继续补充想法并重新生成」「保存作品」。

视觉要求：
浅紫和粉色氛围，漫画格、画笔、相纸边框。温柔亲子，不要夸张战斗漫画风。

禁止：
不要选择题表单，不要独立预览页，不要真人还原承诺，不要猫咖/睡眠元素。
```

## 9. 日记卡生成页

```text
为「时光书」生成移动端 H5 UI 页面设计稿。

产品说明：
「时光书」日记卡生成页是独立工作台。用户可以从时间轴带入回忆，也可以直接聊天补充素材。页面通过多轮聊天确认主图、标题、短句和卡片氛围。生成完成后在本页切换为日记卡预览状态。

页面名称：
「日记卡生成页」

页面目标：
生成一张可保存和分享的亲子回忆日记卡。

页面结构：
1. 顶部导航栏：
   - 返回箭头。
   - 标题「生成日记卡」。
   - 右侧小字「先聊重点」。

2. 可选素材卡：
   - 显示已带入回忆或空素材提示。

3. 日记卡草稿骨架：
   - 竖版小卡片。
   - 包含主图占位、日期、标题、短文区域。
   - 标注「预览骨架，不是最终结果」。

4. 图片确认区：
   - 当前主图以相纸展示。
   - 小按钮「换主图」。

5. 多轮聊天区：
   - AI：「这张卡片最想保留哪一句话？」
   - 用户：「那一刻，我突然觉得你真的长大了。」
   - AI：「标题想温柔一点，还是纪念感强一点？」
   - 用户：「温柔一点。」

6. 底部输入区：
   - placeholder「补充卡片上想写的话」。
   - 发送按钮。

7. 底部固定主按钮：
   - 「确认生成日记卡」。

8. 本页预览状态：
   - 生成后同页展示竖版日记卡成品。
   - 底部按钮「继续补充想法并重新生成」「保存作品」。

视觉要求：
像制作一张亲子纪念卡。使用回忆书页背景、相纸框、便签纸、小花和星星装饰。

禁止：
不要模板选择题，不要复杂表单，不要独立预览页，不要支付信息。
```

## 10. 我的页面

```text
为「时光书」生成移动端 H5 UI 页面设计稿。

产品说明：
「我的」页面只查看历史生成作品，不作为创作入口。用户可以按文章、漫画、日记卡查看已保存作品。

页面名称：
「我的页面」

页面目标：
展示用户历史生成作品，按类型分类浏览。不要提供新的生成入口。

页面结构：
1. 顶部导航栏：
   - 标题「我的」。
   - 右侧设置图标或留空。

2. 用户摘要卡：
   - 亲子小头像。
   - 文案「安安的作品集」。
   - 数据「已保存 8 件作品」。

3. 分类 Tab：
   - 「文章」「漫画」「日记卡」。
   - 当前选中「文章」。

4. 作品列表：
   - 每个作品卡片显示：
     - 缩略预览。
     - 标题。
     - 生成时间。
     - 关联回忆，例如「来自：毕业那天的拥抱」。
     - 按钮「查看」。
   - 示例文章卡：「写给毕业那天的你」。
   - 示例漫画卡：「毕业拥抱的小漫画」。
   - 示例日记卡：「那一刻你长大了」。

5. 空分类状态：
   - 如果某分类为空，文案「还没有保存的作品」。
   - 不提供生成按钮，只提示「去生成页创作后会出现在这里」。

6. 底部主导航：
   - 「时间轴」「生成」「我的」。
   - 当前选中「我的」。

视觉要求：
像温柔的作品收藏夹。使用相册纸、书页卡、浅粉背景、柔和分类 Tab。

禁止：
不要把我的页面做成生成入口页。不要出现「立即生成」主按钮。不要支付/会员信息。
```

## 11. 生成页内预览状态

```text
为「时光书」生成移动端 H5 UI 状态稿。

产品说明：
三个生成页生成完成后，不跳独立预览页，而是在当前页面内切换为预览状态。这个状态可用于文章生成页、漫画生成页、日记卡生成页。

页面名称：
「生成页内预览状态」

页面目标：
展示生成成品，并允许用户继续补充想法重新生成或保存作品。

页面结构：
1. 顶部导航栏：
   - 返回箭头。
   - 标题根据类型变化：「生成文章」「生成漫画」「生成日记卡」。
   - 状态标签「已生成」。

2. 成功提示卡：
   - 时光书小精灵插画。
   - 文案「这段回忆已经变成作品」。

3. 成品预览区：
   - 文章：白色书页，标题和正文。
   - 漫画：柔和漫画图，像放在桌面的相纸。
   - 日记卡：竖版卡片，图片、日期、标题、短文。
   - 画面可以以日记卡为主示例，但要保留三种类型的适配感。

4. 补充想法区：
   - 输入条 placeholder「还想调整哪里？比如更温柔、更像妈妈说的话」。
   - 按钮「补充并重生成」。

5. 底部操作：
   - 主按钮「保存作品」。
   - 次按钮「返回生成」。

视觉要求：
预览区像一页放在相册桌面上的时光书作品。背景温柔、有成品仪式感。

禁止：
不要做成独立预览页路由感。不要纯文本结果页，不要参数设置页，不要支付/会员信息。
```

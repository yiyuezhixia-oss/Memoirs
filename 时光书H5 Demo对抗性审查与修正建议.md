# 时光书 H5 Demo 对抗性审查与修正建议

## 1. 当前权威架构

本审查以最新信息架构为准：

1. 底部主导航：`时间轴 / 生成 / 我的`。
2. `时间轴` 只负责回忆日志沉淀、浏览、新增、AI 整理快捷入口。
3. `生成` 是独立创作区，包含 AI整理回忆、文章、漫画、日记卡四个入口。
4. 文章、漫画、日记卡三个生成页都通过多轮聊天确认想法，不是选择题表单。
5. 生成完成后在当前生成页内切换预览状态。
6. `我的` 只查看历史作品，不作为生成入口。

## 2. 信息架构风险

### 2.1 用户误以为只能从时间轴生成

反例：用户进入时间轴，找不到文章/漫画/日记卡的大入口，以为必须先创建一条日志才能生成。

风险：P0

修正：

1. 底部导航必须有清晰 `生成` Tab。
2. 生成首页必须同时显示 `AI整理回忆` 和三个作品生成入口。
3. 生成首页必须说明“可以直接聊天补充素材，也可以从时间轴带入回忆”。
4. 时间轴卡片按钮命名为 `用于生成`，不要叫 `生成作品`，避免把生成流程误解成时间轴附属流程。

### 2.2 用户误以为我的页面能开始生成

反例：用户在我的页面看到文章/漫画/日记卡分类，以为这里可以创建新作品。

风险：P1

修正：

1. 我的页面不放 `立即生成` 主按钮。
2. 空状态只提示“去生成页创作后会出现在这里”。
3. 我的页面作品卡只提供 `查看`。

### 2.3 生成页像表单而不是聊天工作台

反例：页面放了用途、长度、风格、模板等一堆选择项，违背“先基于聊天确认用户想法”的产品意图。

风险：P0

修正：

1. 三个生成页主体必须是聊天区。
2. 选择项只能作为 AI 追问内容的结果展示，不做主交互。
3. 底部按钮是 `确认生成文章/漫画/日记卡`，不是“提交表单”。

## 3. 时间轴风险

### 3.1 时间轴卡片功能过多

反例：每张卡片同时放查看、编辑、生成文章、生成漫画、生成日记卡，手机上显得拥挤。

风险：P1

修正：

1. 卡片只放一个 `用于生成` 次按钮。
2. 点击后可以选择类型并进入对应生成工作台。
3. 主按钮仍应是时间轴自身操作：新增、AI 整理。

### 3.2 AI 整理聊天和生成聊天混淆

反例：用户不知道 `AI整理回忆` 和 `生成文章页聊天` 有什么区别。

风险：P1

修正：

1. AI 整理页文案明确：`整理成时间轴日志`。
2. 生成页文案明确：`确认作品想法并生成`。
3. 生成页聊天第一屏必须显示当前作品类型。

## 4. 生成页风险

### 4.1 无来源素材时页面空

反例：用户从生成首页直接进入文章生成页，没有时间轴来源，页面顶部空白或提示错误。

风险：P0

修正：

1. 生成页必须支持无 `sourceEntryId`。
2. 无来源时显示提示卡：`还没有带入回忆，可以直接聊天补充素材`。
3. 第一条 AI 问题承担素材采集职责。

### 4.2 有来源素材时用户不知道可以继续补充

反例：从时间轴带入毕业日志后，用户以为素材固定，不能再说想法。

风险：P1

修正：

1. 来源卡下方写：`可以继续补充你想表达的重点`。
2. 聊天问题要围绕“这条回忆你想怎么变成作品”。

### 4.3 生成完成后跳走破坏上下文

反例：生成成功后跳到独立结果页，用户找不到刚才聊天，也不知道怎么补充重生成。

风险：P0

修正：

1. 生成结果在当前生成页内展示。
2. 页面保留 `继续补充想法并重生成`。
3. 聊天记录与结果同属一个 `generationTask`。

### 4.4 保存作品后我的页面没有结果

反例：用户点保存作品后去我的页面，列表为空。

风险：P0

修正：

1. 保存作品必须写入 `generationTasks.result` 和 `saved=true`。
2. 我的页面只读取 `saved=true` 的任务。
3. 分类由 `type=article | comic | diary_card` 决定。

## 5. 数据关系风险

### 5.1 生成聊天归档范围过大

反例：Demo 第一阶段就要把生成页聊天同时归作品和时间轴，开发量变大。

风险：P1

修正：

1. 第一阶段只保存到生成作品。
2. 第二阶段再支持“归入时间轴”。
3. 数据结构预留 `sourceEntryId` 和 `relatedGenerationIds`，但不强制实现双向归档。

### 5.2 作品和来源日志关联丢失

反例：从时间轴带入日志生成漫画后，我的页面看不到“来自哪段回忆”。

风险：P1

修正：

1. 生成任务保存 `sourceEntryId`。
2. 我的作品卡展示 `来自：xxx`。
3. 无来源作品显示 `来自：生成页聊天`。

## 6. UI 风险

### 6.1 生成首页像营销页

反例：顶部大 hero 和宣传语太重，用户不知道能点哪里创作。

风险：P1

修正：

1. 生成首页首屏必须露出 AI整理回忆和三个作品生成入口。
2. 横幅只做情绪引导，不能压过入口。

### 6.2 我的页面像生成页

反例：我的页面分类卡片做得像三个大入口，用户误点。

风险：P1

修正：

1. 我的页面使用作品列表和历史卡片，不用大创作入口卡。
2. Tab 是过滤器，不是功能入口。

### 6.3 聊天页像普通 IM

反例：气泡、头像、输入框做成微信风，产品记忆书气质消失。

风险：P2

修正：

1. AI 气泡像采访纸条。
2. 用户气泡保留轻柔背景。
3. 页面使用书页、相纸、时光书小精灵点缀。

## 7. 必须覆盖的验收

1. 从底部 `生成` 进入文章生成页，不选时间轴日志，也能聊天并生成本页预览。
2. 从时间轴某条日志点击 `用于生成`，选择漫画，进入漫画生成页并显示已带入素材。
3. 日记卡生成页生成完成后，同页展示竖版日记卡预览。
4. 我的页面只显示已保存作品，不出现生成入口。
5. 保存作品后，我的页面对应分类能看到作品卡。
6. AI 整理回忆聊天写入时间轴后，不会重复整理同一批消息。
